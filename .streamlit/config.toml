[theme]
primaryColor = "#2563eb"          # medical blue — signal accents / buttons
backgroundColor = "#ffffff"
secondaryBackgroundColor = "#f1f5f9"  # slate-100 cards
textColor = "#0f172a"             # slate-900
font = "sans serif"

[server]
enableStaticServing = true
